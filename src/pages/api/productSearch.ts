export class productSearch{
    searchQuery: string;
    searchType: string;
 constructor(searchQuery: string,   searchType:string) {
        this.searchQuery = searchQuery;
        this.searchType=searchType;
   }
}